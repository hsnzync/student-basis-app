const mutations = {
    SET_INITIAL_SUBJECT(state, subject) {
        state.activeSubject = subject
    },
    SET_SELECTED_SUBJECT(state, subject) {
        state.activeSubject = subject
    }
}

export default mutations
